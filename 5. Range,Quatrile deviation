5 measures of dipersion
a) Range

r <- scan()
l <- length(r)
for (i in 1:(l-1)){
  for (j in (i+1):l) {
    if(r[i]>r[j]){
      temp=r[i]
      r[i]=r[j]
      r[j]=temp
    }
  }
}
Range <- r[l]-c[1]
cat("range of given vector is:",Range)

cat("range from bultin fn is:",range(r))
